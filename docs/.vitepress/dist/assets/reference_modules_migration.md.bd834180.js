import{_ as d,K as o,o as l,c as h,O as a,w as i,k as e,a as t,$ as s}from"./chunks/framework.9fa1e75e.js";const x=JSON.parse('{"title":"@apostrophecms/migration","description":"","frontmatter":{"extends":"@apostrophecms/module"},"headers":[],"relativePath":"reference/modules/migration.md","filePath":"reference/modules/migration.md","lastUpdated":1690289066000}'),m={name:"reference/modules/migration.md"},u=e("h1",{id:"apostrophecms-migration",tabindex:"-1"},[e("code",null,"@apostrophecms/migration"),t(),e("a",{class:"header-anchor",href:"#apostrophecms-migration","aria-label":'Permalink to "`@apostrophecms/migration`"'},"​")],-1),p=e("p",null,[e("strong",null,"Alias:"),t(),e("code",null,"apos.migration")],-1),f=s('<p>This module provides services for database migrations. These <strong>migrations are used to make changes to the database</strong> at the time of a new code deployment, typically because of <em>data structure changes</em> in code or <em>to fix data errors</em>. This is completely separate from transferring data between environments or between major versions of Apostrophe.</p><p>The <code>@apostrophecms/migration:migrate</code> task carries out all migrations that have been registered with this module, though typically only new migrations will run (see the warning below). In development environments all new migrations also run on every site startup.</p><h2 id="featured-methods" tabindex="-1">Featured methods <a class="header-anchor" href="#featured-methods" aria-label="Permalink to &quot;Featured methods&quot;">​</a></h2><p>The following methods belong to this module and may be useful in project-level code. See the <a href="https://github.com/apostrophecms/apostrophe/blob/main/modules/%40apostrophecms/migration/index.js" target="_blank" rel="noreferrer">source code</a> for all methods that belong to this module.</p><p>Because this module has an alias, you can call these from another module from the alias path. For example, <code>self.apos.migration.add()</code>.</p><h3 id="add-name-fn" tabindex="-1"><code>add(name, fn)</code> <a class="header-anchor" href="#add-name-fn" aria-label="Permalink to &quot;`add(name, fn)`&quot;">​</a></h3><p>Add a migration function (<code>fn</code>) to be invoked when the <code>@apostrophecms/migration:migrate</code> task is invoked. Each migration is only invoked once, however they will all be invoked on a brand-new site, so they must check whether a change is actually needed before executing changes. They must also be harmless to run twice.</p><p>Functions receive no arguments, but have access to the modules where they are registered (via <code>self</code>) and the Apostrophe object (<code>self.apos</code>).</p>',8),g=s('<div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>If running multiple instances of the website on a server, note that previous instances of the site are not stopped automatically while migrations are run. Migrations must minimize their impact on currently running instances of older versions of the site. It is safer to copy data to new properties and to not <em>remove</em> existing data until a subsequent migration.</p></div><h3 id="async-eachdoc-criteria-limit-iterator" tabindex="-1"><code>async eachDoc(criteria, limit, iterator)</code> <a class="header-anchor" href="#async-eachdoc-criteria-limit-iterator" aria-label="Permalink to &quot;`async eachDoc(criteria, limit, iterator)`&quot;">​</a></h3><p>Invokes the <code>iterator</code> function once for each doc in the <code>aposDocs</code> collection. The iterator function receives the document as an argument and is run as an <code>async</code> function. This method will never visit the same doc twice in a single call, even if modifications are made.</p><p>The <code>criteria</code> object is used to find documents to process, using the same format as in a <a href="https://docs.mongodb.com/v4.4/reference/method/db.collection.find/" target="_blank" rel="noreferrer">MongoDB <code>find</code> operation query</a>. <code>limit</code> should be an integer and the number of documents to process in parallel, though it may be omitted. If only two arguments are passed in, <code>limit</code> is assumed to be 1 (only one doc may be processed at a time).</p><p><strong>Note:</strong> This API is meant for migrations and task use only. It has no built-in security checks.</p><h3 id="async-each-collection-criteria-limit-iterator" tabindex="-1"><code>async each(collection, criteria, limit, iterator)</code> <a class="header-anchor" href="#async-each-collection-criteria-limit-iterator" aria-label="Permalink to &quot;`async each(collection, criteria, limit, iterator)`&quot;">​</a></h3><p>This method is similar to <a href="#async-eachdoc-criteria-limit-iterator"><code>eachDoc</code></a>, but it also accepts a database collection as its first argument. When working on normal website content, <code>eachDoc</code> will be better to use, though this method can be useful if operating on other database collections, such as the attachments collection (<code>self.apos.attachment.db</code>).</p><p><strong>Note:</strong> This API is meant for migrations and task use only. It has no built-in security checks.</p><h3 id="async-eacharea-criteria-limit-iterator" tabindex="-1"><code>async eachArea(criteria, limit, iterator)</code> <a class="header-anchor" href="#async-eacharea-criteria-limit-iterator" aria-label="Permalink to &quot;`async eachArea(criteria, limit, iterator)`&quot;">​</a></h3><p>Invokes the <code>iterator</code> function once for <em>every area</em> in <em>every doc</em> in the <code>aposDocs</code> collection. This method will never visit the same doc twice in a single call, even if modifications are made.</p><p>The <code>iterator</code> function receives the following arguments:</p><ul><li><code>doc</code>: The full document object</li><li><code>area</code>: The area object within the document</li><li><code>dotPath</code>: The dot notation series leading to the area within the doc. If the area is a top-level field on the doc type&#39;s schema this will simply be the field name.</li></ul><p><code>criteria</code> may be used to limit the docs for which this is done, similar to its use in <a href="#async-eachdoc-criteria-limit-iterator"><code>eachDoc</code></a>. <code>limit</code> should be an integer and the number of documents to process in parallel, though it may be omitted. If only two arguments are passed in, <code>limit</code> is assumed to be 1 (only one doc may be processed at a time).</p><p><strong>Note:</strong> This API is meant for migrations and task use only. It has no built-in security checks.</p><h3 id="async-eachwidget-criteria-limit-iterator" tabindex="-1"><code>async eachWidget(criteria, limit, iterator)</code> <a class="header-anchor" href="#async-eachwidget-criteria-limit-iterator" aria-label="Permalink to &quot;`async eachWidget(criteria, limit, iterator)`&quot;">​</a></h3><p>Continuing from <code>eachArea()</code>, this method goes one level deeper. Invokes the <code>iterator</code> function once for <em>every widget</em> in <em>every area</em> in <em>every doc</em> in the <code>aposDocs</code> collection. This method will never visit the same doc twice in a single call, even if modifications are made.</p><p>The <code>iterator</code> function receives the following arguments:</p><ul><li><code>doc</code>: The full document object</li><li><code>widget</code>: The widget object within the document</li><li><code>dotPath</code>: The dot notation series leading to the widget within the doc. If the area is a top-level field on the doc type&#39;s schema this will simply be the field name.</li></ul><p><code>criteria</code> may be used to limit the docs for which this is done, similar to its use in <a href="#async-eachdoc-criteria-limit-iterator"><code>eachDoc</code></a>. <code>limit</code> should be an integer and the number of documents to process in parallel, though it may be omitted. If only two arguments are passed in, <code>limit</code> is assumed to be 1 (only one doc may be processed at a time).</p><p><strong>Note:</strong> This API is meant for migrations and task use only. It has no built-in security checks.</p><h2 id="module-tasks" tabindex="-1">Module tasks <a class="header-anchor" href="#module-tasks" aria-label="Permalink to &quot;Module tasks&quot;">​</a></h2><h3 id="migrate" tabindex="-1"><code>migrate</code> <a class="header-anchor" href="#migrate" aria-label="Permalink to &quot;`migrate`&quot;">​</a></h3><p>Full command: <code>node app @apostrophecms/migration:migrate</code></p><p>Run this command-line task to run all migrations. Migrations do not run automatically in production environments, so in that context this must be run manually or as part of a deployment process.</p>',24);function b(n,y,w,v,_,k){const r=o("AposRefExtends"),c=o("AposCodeBlock");return l(),h("div",null,[u,p,a(r,{module:n.$frontmatter.extends},null,8,["module"]),f,a(c,null,{caption:i(()=>[t(" modules/rose/index.js ")]),default:i(()=>[t(" ```javascript module.exports = { extend: '@apostrophecms/piece-type' init(self) { // 👇 Adding a data migration related to this module. self.apos.migration.add('fix-roses', self.paintRosesRed); }, methods(self) { return { // 👇 Registering a method to run in the migration. async paintRosesRed () { await self.apos.migration.eachDoc({ type: 'rose', color: 'white' }, async (doc) => { await self.apos.doc.db.updateOne({ _id: doc._id }, { $set: { color: 'red' } }); }); } } } }; ``` ")]),_:1}),g])}const I=d(m,[["render",b]]);export{x as __pageData,I as default};
