import{_ as e,o as a,c as o,$ as t}from"./chunks/framework.9fa1e75e.js";const m=JSON.parse('{"title":"Glossary","description":"","frontmatter":{},"headers":[],"relativePath":"reference/glossary.md","filePath":"reference/glossary.md","lastUpdated":1690289066000}'),s={name:"reference/glossary.md"},n=t(`<h1 id="glossary" tabindex="-1">Glossary <a class="header-anchor" href="#glossary" aria-label="Permalink to &quot;Glossary&quot;">​</a></h1><p>While these terms have more common definitions, these descriptions focus on each term&#39;s use in the context of Apostrophe.</p><h2 id="general-terms" tabindex="-1">General terms <a class="header-anchor" href="#general-terms" aria-label="Permalink to &quot;General terms&quot;">​</a></h2><h3 id="core" tabindex="-1">Core <a class="header-anchor" href="#core" aria-label="Permalink to &quot;Core&quot;">​</a></h3><p>The term &quot;Apostrophe core,&quot; or simply &quot;core,&quot; is used frequently throughout this documentation. &quot;Core&quot; refers to the foundational software for all Apostrophe projects. It is the package that a developer would get when installing with <code>npm i apostrophe</code> or <code>yarn add apostrophe</code>. This documentation focuses on the features in Apostrophe core since those are the features that all Apostrophe projects have in common.</p><h3 id="module" tabindex="-1">Module <a class="header-anchor" href="#module" aria-label="Permalink to &quot;Module&quot;">​</a></h3><p>Apostrophe sites are powered by Apostrophe modules. Each module is responsible for providing one feature, such as a type of widget, a type of customized page, or a service like pushing assets to the browser. Apostrophe has many standard modules which provide its core features.</p><p>A module can extend, or &quot;subclass&quot;, another module, and most of the modules you create at &quot;project level&quot; (in your project code) will. All modules implicitly extend <code>@apostrophecms/module</code>, which provides a rich set of conveniences, like rendering templates or pushing assets relative to your module&#39;s folder.</p><p>Modules created at project level live in subdirectories of <code>/modules</code>. For example, an <code>article</code> module would be managed in the <code>/modules/article</code> directory. Code and configuration for a module lives in it&#39;s <code>index.js</code> file (<code>/modules/article/index.js</code>, in the example).</p><h3 id="doc" tabindex="-1">Doc <a class="header-anchor" href="#doc" aria-label="Permalink to &quot;Doc&quot;">​</a></h3><p>A &quot;doc&quot; refers to a content-related document in Apostrophe&#39;s database. They are in the <code>aposDocs</code> MongoDB collection.</p><p>Docs contain various properties, including <a href="#area">areas</a>, as described by a schema configured for their <strong>document types</strong> (or &quot;doc type&quot;). Each doc type will extend <code>@apostrophecms/piece-type</code>, <code>@apostrophecms/page-type</code>, <code>@apostrophecms/piece-page-type</code>, or another doc type that itself extends one of those core modules.</p><p>At a minimum, a doc has unique <code>_id</code> and <code>slug</code> properties. The <code>type</code> property identifies the doc type and determines what other behaviors it might have. The <code>@apostrophecms/doc</code> module provides methods for working with docs, including the <code>apos.docs.getManager(doc.type)</code> method, which returns a reference to the module suited to working with that type of document.</p><h3 id="piece" tabindex="-1">Piece <a class="header-anchor" href="#piece" aria-label="Permalink to &quot;Piece&quot;">​</a></h3><p>A &quot;piece&quot; is a unit of standalone, structured, non-page content in Apostrophe. A piece module extends <code>@apostrophecms/piece-type</code>. These include core elements such as images and users, as well as project-specific content, such as events or products.</p><p>They are not pages themselves, but can be displayed at individual page URLs if there is a matching pieces page type (extending the <code>@apostrophecms/piece-page-type</code> module). They could then have individual <a href="#show-page">show pages</a> once an <a href="#index-page">index page</a> (an often-paginated listing page) of that type is created.</p><h3 id="page" tabindex="-1">Page <a class="header-anchor" href="#page" aria-label="Permalink to &quot;Page&quot;">​</a></h3><p>A doc that is part of the page tree and is designed to be viewed like any traditional web page. It can potentially have &quot;child&quot; pages via the page tree hierarchy. Pages belong to particular &quot;page types,&quot; and may have custom fields based on their type, but are not displayed in an index listing as pieces are.</p><p>Their <code>slug</code> property begins with <code>/</code>, which is only true for pages. A page also has <code>path</code>, <code>rank</code> and <code>depth</code> properties that help make its relationship to other pages in the tree clear.</p><p><code>path</code> differs from <code>slug</code> in that it always reflects the true parent-child relationships between pages in the tree, while <code>slug</code> can be edited and shortened if desired so that URLs don&#39;t have to contain a lot of slashes to reach a deep page.</p><h3 id="index-page" tabindex="-1">Index page <a class="header-anchor" href="#index-page" aria-label="Permalink to &quot;Index page&quot;">​</a></h3><p>An index page, is a special <a href="#page">page</a> that is built to display a listing of <a href="#piece">pieces</a>. Index pages extend the <code>@apostrophecms/piece-page-type</code> module. In addition to data available to all pages, index page templates have access to a <code>data.pieces</code> array, which contains objects representing individual pieces, limited by the pieces page <code>perPage</code> option and the state of pagination. Index pages also have <code>data.currentPage</code> and <code>data.totalPages</code> number values available to support pagination.</p><h3 id="show-page" tabindex="-1">Show page <a class="header-anchor" href="#show-page" aria-label="Permalink to &quot;Show page&quot;">​</a></h3><p>Despite their name, these are not <a href="#page">pages</a> in the sense of extending the <code>@apostrophecms/page-type</code> module. They therefore do not have individual representation in the page tree. They are a feature, enabled by creating an <a href="#index-page">index page</a>, providing individual URLs that display a rendered template for individual <a href="#piece">pieces</a>. <strong>A blog listing is an &quot;index page&quot; and the individual blog post is a &quot;show page.&quot;</strong></p><p>Show page templates (<code>show.html</code>), do have access to their parent index page&#39;s <code>data.page</code> object, but they also have access to <code>data.piece</code>, which contains all of the data for a particular piece.</p><h3 id="global-doc" tabindex="-1">Global doc <a class="header-anchor" href="#global-doc" aria-label="Permalink to &quot;Global doc&quot;">​</a></h3><p>There is a single doc with the slug, <code>global</code>, which is always loaded and available to page templates as <code>data.global</code>. This is useful for shared, site-wide headers and footers that are editable, etc. It is managed by the <code>@apostrophecms/global</code> module.</p><h3 id="slug" tabindex="-1">Slug <a class="header-anchor" href="#slug" aria-label="Permalink to &quot;Slug&quot;">​</a></h3><p>The slug of an Apostrophe doc is a string that uniquely identifies that doc within the database. Apostrophe will enforce that no two docs have the same slug. Slugs <em>can</em> be changed, unlike the <code>_id</code> property of the database document. The Apostrophe user interface and REST API will suggest slugs automatically based on the document&#39;s title on creation.</p><p>If the doc is a page, or a piece with a <a href="#show-page">show page</a>, the slug is used as a component of the page URL. This works differently for pages and pieces.</p><ul><li><a href="#page">Page</a> slugs begin with a forward slash and, by default, include sections for their parent pages, e.g., <code>/previous-page/new-page</code> for a page titled &quot;New Page&quot; with a parent page of &quot;Previous Page.&quot; Editing a page slug will not effect the actual page tree structure.</li><li><a href="#piece">Piece</a> slugs do not begin with a forward slash nor do they include reference to an <a href="#index-page">index page</a>. A piece titled &quot;New Piece&quot; will get the suggested slug of <code>new-piece</code> if no other doc has that slug already.</li></ul><h3 id="widget" tabindex="-1">Widget <a class="header-anchor" href="#widget" aria-label="Permalink to &quot;Widget&quot;">​</a></h3><p>A widget is a single section of content in an <a href="#area">area</a> that can usually be edited, such as a block of rich text, an image slideshow, or a callout block. You can create entirely new types of widgets by extending the <code>@apostrophecms/widget-type</code> module.</p><h3 id="area" tabindex="-1">Area <a class="header-anchor" href="#area" aria-label="Permalink to &quot;Area&quot;">​</a></h3><p>An area is a special field in a piece or page where you can add any number of widgets of any type, as configured for that piece or page type. Editors can add, edit, move, and remove widgets within the area. Widgets in the area maintain their order for consistent displaying.</p><p>Areas are inserted into your templates using the <a href="/guide/areas-and-widgets.html"><code>area</code> template tag</a>. They are implemented by the <code>@apostrophecms/area</code> module.</p><h3 id="template" tabindex="-1">Template <a class="header-anchor" href="#template" aria-label="Permalink to &quot;Template&quot;">​</a></h3><p>Apostrophe uses the <a href="https://mozilla.github.io/nunjucks/" target="_blank" rel="noreferrer">Nunjucks template language</a> to render webpages and components of pages, such as widgets and <a href="#block">blocks</a>. Nunjucks shares a syntax with the Jinja and Twig languages which are popular in the Python and PHP worlds, respectively.</p><h3 id="block" tabindex="-1">Block <a class="header-anchor" href="#block" aria-label="Permalink to &quot;Block&quot;">​</a></h3><p>In Nunjucks templates, <a href="https://mozilla.github.io/nunjucks/templating.html#block" target="_blank" rel="noreferrer">a block is a section of a template that can be overridden in a template that extends it</a>. This is a useful technique in page templates when paired with <a href="https://mozilla.github.io/nunjucks/templating.html#template-inheritance" target="_blank" rel="noreferrer">template inheritance</a>.</p><h3 id="schema" tabindex="-1">Schema <a class="header-anchor" href="#schema" aria-label="Permalink to &quot;Schema&quot;">​</a></h3><p>A piece, page, or widget type&#39;s schema is the definition of its fields. Apostrophe&#39;s schemas are used both to create an editing interface and to sanitize and save data on the server side. Commonly used field types include strings, integers, floats, select elements and &quot;relationship&quot; fields, which allow editors to define connections to other docs.</p><p>Schemas are built by using the <code>add</code>, <code>remove</code>, and <code>group</code> properites of the <code>fields</code> option when configuring any module that extends <code>@apostrophecms/piece-type</code>, <code>@apostrophecms/page-type</code>, or <code>@apostrophecms/widget-type</code>.</p><p>Here is a simple example in which we add a required &quot;author&quot; string field to the schema for &quot;story,&quot; a custom piece type:</p><div class="language-javascript"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#676E95;font-style:italic;">// modules/story/index.js in our project</span></span>
<span class="line"><span style="color:#89DDFF;">module.exports</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#F07178;">extend</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">apostrophe-pieces</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#F07178;">name</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">story</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#F07178;">fields</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#F07178;">add</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">      </span><span style="color:#F07178;">author</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#F07178;">type</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">string</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#F07178;">required</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#FF9CAC;">true</span></span>
<span class="line"><span style="color:#A6ACCD;">      </span><span style="color:#89DDFF;">}</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">}</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#89DDFF;">}</span></span>
<span class="line"><span style="color:#89DDFF;">}</span></span></code></pre></div><h3 id="relationship" tabindex="-1">Relationship <a class="header-anchor" href="#relationship" aria-label="Permalink to &quot;Relationship&quot;">​</a></h3><p>In <a href="#schema">Apostrophe schemas</a>, a &quot;relationship&quot; field describes a connection with another doc. A development agency might want to connect &quot;services&quot; to &quot;projects,&quot; or you might want to select &quot;office&quot; pieces for display in the website footer.</p><p>In that second case, the schema containing the relationship field might look like this:</p><div class="language-javascript"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#FFCB6B;">fields</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#F07178;">  </span><span style="color:#FFCB6B;">add</span><span style="color:#89DDFF;">:</span><span style="color:#F07178;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#F07178;">    </span><span style="color:#FFCB6B;">_offices</span><span style="color:#89DDFF;">:</span><span style="color:#F07178;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#F07178;">      </span><span style="color:#FFCB6B;">label</span><span style="color:#89DDFF;">:</span><span style="color:#F07178;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">Offices</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#F07178;">      </span><span style="color:#FFCB6B;">type</span><span style="color:#89DDFF;">:</span><span style="color:#F07178;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">relationship</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#F07178;">      </span><span style="color:#FFCB6B;">withType</span><span style="color:#89DDFF;">:</span><span style="color:#F07178;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">office</span><span style="color:#89DDFF;">&#39;</span></span>
<span class="line"><span style="color:#F07178;">    </span><span style="color:#89DDFF;">}</span></span>
<span class="line"><span style="color:#F07178;">  </span><span style="color:#89DDFF;">}</span></span>
<span class="line"><span style="color:#89DDFF;">}</span></span></code></pre></div><p>The editing interface for this join allows the user to pick offices to associate with this service. When writing templates, the developer is then able to access a <code>._offices</code> array as a property of each service.</p><h3 id="shortname" tabindex="-1">Shortname <a class="header-anchor" href="#shortname" aria-label="Permalink to &quot;Shortname&quot;">​</a></h3><p>An Apostrophe website&#39;s shortname is used as the primary identifier for the application. It is used as the Express session name and by default is the database name and part of the CSRF cookie name. It is a top-level property of the application object we <a href="/guide/setting-up.html#creating-a-project">configure in a project&#39;s <code>app.js</code> file</a> as <code>shortName</code>.</p><h2 id="user-interface-terms" tabindex="-1">User interface terms <a class="header-anchor" href="#user-interface-terms" aria-label="Permalink to &quot;User interface terms&quot;">​</a></h2><h3 id="manager" tabindex="-1">Manager <a class="header-anchor" href="#manager" aria-label="Permalink to &quot;Manager&quot;">​</a></h3><p>The manager, or manager modal, is a UI modal that displays a table or list of pages or pieces of a particular doc type. Pages are displayed in one manager and every piece type is displayed in their own manager modals. This UI is usually opened by clicking or triggering the doc type&#39;s button in the admin bar. The manager is also used as a &quot;chooser&quot; when browsing pages or pieces for a relationship field.</p><h3 id="editor" tabindex="-1">Editor <a class="header-anchor" href="#editor" aria-label="Permalink to &quot;Editor&quot;">​</a></h3><p>The editor modal is a traditional interface with <a href="/reference/field-types/">discrete fields</a> to add or update a document&#39;s content. This is distinct from the &quot;in-context&quot; editing experience where editors update content in the page as any visitor would see it.</p><h2 id="localization-terms" tabindex="-1">Localization terms <a class="header-anchor" href="#localization-terms" aria-label="Permalink to &quot;Localization terms&quot;">​</a></h2><p>Apostrophe generally subscribes to <a href="https://www.w3.org/International/questions/qa-i18n.en" target="_blank" rel="noreferrer">the W3C definitions</a> of the following terms. These definitions borrow from their descriptions with a focus on use with Apostrophe.</p><h3 id="localization" tabindex="-1">Localization <a class="header-anchor" href="#localization" aria-label="Permalink to &quot;Localization&quot;">​</a></h3><p>&quot;Localization,&quot; often abbreviated as <strong>l10n</strong>, is the process of adapting the Apostrophe user interface and Apostrophe website content for different groups of people (usually based on language and country). This can include text translation, date formats, content variation, and much more.</p><h3 id="locale" tabindex="-1">Locale <a class="header-anchor" href="#locale" aria-label="Permalink to &quot;Locale&quot;">​</a></h3><p>A locale is a content classification used to associate certain content and l10n settings with a group of people. Locales may be languages (e.g., English, Spanish), countries (e.g., Canada, France), combinations of the two (e.g., Canadian French, Canadian English), or really any significant group of website visitors.</p><h3 id="internationalization" tabindex="-1">Internationalization <a class="header-anchor" href="#internationalization" aria-label="Permalink to &quot;Internationalization&quot;">​</a></h3><p>&quot;Internationalization,&quot; often abbreviated as <strong>i18n</strong>, refers to the whole system that supports localization. This is why the primary core Apostrophe module related to these topics is <code>@apostrophecms/i18n</code>. We can think of i18n as the tools and l10n as how people use them to make content more accessible to different peoples.</p>`,65),i=[n];function r(l,p,c,d,h,u){return a(),o("div",null,i)}const y=e(s,[["render",r]]);export{m as __pageData,y as default};
