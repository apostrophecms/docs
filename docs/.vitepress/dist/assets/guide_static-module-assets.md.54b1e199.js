import{_ as e,o as s,c as t,$ as o}from"./chunks/framework.9fa1e75e.js";const b=JSON.parse('{"title":"Using static module assets","description":"","frontmatter":{"prev":{"text":"Nested module folders","link":"guide/nested-module-subdirs.md"},"next":{"text":"Schema fields | Content fields","link":"guide/content-schema.md"}},"headers":[],"relativePath":"guide/static-module-assets.md","filePath":"guide/static-module-assets.md","lastUpdated":1690289066000}'),a={name:"guide/static-module-assets.md"},i=o('<h1 id="using-static-module-assets" tabindex="-1">Using static module assets <a class="header-anchor" href="#using-static-module-assets" aria-label="Permalink to &quot;Using static module assets&quot;">​</a></h1><p>While developers can place static files in the top-level <code>/public</code> folder of the project, which allows a file like <code>/public/images/bg.png</code> to be accessed via the URL <code>https://example.com/images/bg.png</code>, this isn&#39;t a good solution for some situations. One issue is that the URL does not change with each new deployment, so if <code>bg.png</code> has been changed a stale version may be served by the browser&#39;s cache.</p><p>Another issue is that those who configure Apostrophe to copy CSS and JS assets to Amazon S3 and other CDNs will discover that code like <code>url(&quot;/images/bg.png&quot;)</code> no longer works, because the static asset files are not on the same host with the CSS code. This can be worked around with absolute URLs, but this gives up some of the benefit of using a CDN.</p><p>Apostrophe offers a solution to this problem: each module folder can have its own <code>public</code> subdirectory (e.g., <code>/modules/article/public</code>). Any static assets in a module&#39;s public subdirectory are <em>copied to the same release location</em> where the CSS and JS bundles are deployed, regardless of whether that is on the server&#39;s hard drive, in S3, or some third location.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The <code>public</code> folder of each module solves a different problem from <a href="/guide/front-end-assets.html#the-ui-public-folder"><code>ui/public</code></a>. While <code>ui/public</code> is for JavaScript and CSS files that should be appended as-is to Apostrophe&#39;s JavaScript and CSS bundles, usually to accommodate a custom webpack build, <code>public</code> is for files that should be <strong>available separately.</strong> A common example is a <code>.png</code> file to be used as a background image in CSS.</p></div><h2 id="automatic-asset-path-correction-in-stylesheets" tabindex="-1">Automatic asset path correction in stylesheets <a class="header-anchor" href="#automatic-asset-path-correction-in-stylesheets" aria-label="Permalink to &quot;Automatic asset path correction in stylesheets&quot;">​</a></h2><p>Files we place in the <code>public</code> subdirectory of any module are always deployed such that we can write URLs like this in our CSS or SCSS files:</p><p><code>/modules/custom-module/images/bg.png</code></p><p>Apostrophe will automatically fix these &quot;asset paths&quot; so they refer to the final URL of the asset, <em>no matter what our production environment looks like.</em> In addition, in production deployments, the final URL will always contain a &quot;release identifier&quot; so that any static assets in the browser cache from a previous release are not reused.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>If you are configuring an Apostrophe core module or other module installed via npm, and you choose to add a <code>public</code> subdirectory for that module at project level, you will need to prefix the module name with <code>my-</code> when creating asset URLs to those assets.</p><p>Note that the <code>my-</code> prefix goes in the module name part, not the organization name part. For example, if you have a <code>modules/@apostrophecms/asset/public/example.svg</code> file, you can access that via CSS or via the methods given below as <code>/modules/@apostrophecms/my-asset/example.svg</code>.</p><p>This distinguishes your <code>public</code> folder from any assets that might be provided by the original npm module.</p><p>It&#39;s usually simpler to just put your public assets in a module specific to your project, rather than a project-level configuration of a core one.</p></div><h2 id="asset-paths-in-nunjucks-templates" tabindex="-1">Asset paths in Nunjucks templates <a class="header-anchor" href="#asset-paths-in-nunjucks-templates" aria-label="Permalink to &quot;Asset paths in Nunjucks templates&quot;">​</a></h2><p>In Nunjucks templates we can convert asset paths to URs by calling the <code>apos.asset.url</code> helper function. Again, the rendered template will include the complete asset URL:</p><div class="language-njk"><button title="Copy Code" class="copy"></button><span class="lang">njk</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">{{ apos.asset.url(&#39;/modules/custom-module/images/bg.png&#39;) }}</span></span></code></pre></div><h2 id="asset-paths-in-front-end-javascript" tabindex="-1">Asset paths in front end JavaScript <a class="header-anchor" href="#asset-paths-in-front-end-javascript" aria-label="Permalink to &quot;Asset paths in front end JavaScript&quot;">​</a></h2><p>Finally, we can obtain the URL corresponding to an asset path in front end JavaScript using <code>apos.util.assetUrl</code>:</p><div class="language-javascript"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">apos</span><span style="color:#89DDFF;">.</span><span style="color:#A6ACCD;">util</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">assetUrl</span><span style="color:#A6ACCD;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">/modules/custom-module/images/bg.png</span><span style="color:#89DDFF;">&#39;</span><span style="color:#A6ACCD;">)</span></span></code></pre></div><h2 id="what-about-extend-and-improve" tabindex="-1">What about <code>extend</code> and <code>improve</code>? <a class="header-anchor" href="#what-about-extend-and-improve" aria-label="Permalink to &quot;What about `extend` and `improve`?&quot;">​</a></h2><p>Sometimes modules extend or improve other modules, inheriting or contributing functionality. If both a base class module <code>a</code> and a module <code>b</code> that extends it contain <code>public</code> folders, all files with unique file names will be available via paths starting with <code>/modules/b</code>. If files provided by <code>a</code> and <code>b</code> have the same name, the version in module <code>b</code> will take precedence in <code>/modules/b</code>. However the base class version is still available in <code>/modules/a</code>. The same logic applies when <code>improve</code> is used.</p>',18),n=[i];function l(c,d,r,p,u,h){return s(),t("div",null,n)}const f=e(a,[["render",l]]);export{b as __pageData,f as default};
