import{_ as e,o as a,c as t,$ as o}from"./chunks/framework.9fa1e75e.js";const g=JSON.parse('{"title":"Query builders","description":"","frontmatter":{"sidebarDepth":2,"prev":{"text":"Server events","link":"reference/server-events.md"},"next":{"text":"📖 REST API Reference","link":"reference/api/README.md"}},"headers":[],"relativePath":"reference/query-builders.md","filePath":"reference/query-builders.md","lastUpdated":1690289066000}'),s={name:"reference/query-builders.md"},r=o('<h1 id="query-builders" tabindex="-1">Query builders <a class="header-anchor" href="#query-builders" aria-label="Permalink to &quot;Query builders&quot;">​</a></h1><p>Server-side database query builders in Apostrophe help developer refine database document queries or refine the query results in some way. The sections below organize core query builders by the relevant document types.</p><p><strong>See the <a href="/guide/database-queries.html">database querying guide</a> for general information about using query builders.</strong></p><h2 id="builders-for-all-doc-types" tabindex="-1">Builders for all doc types <a class="header-anchor" href="#builders-for-all-doc-types" aria-label="Permalink to &quot;Builders for all doc types&quot;">​</a></h2><p>The following query builders apply to all Apostrophe <a href="/reference/glossary.html#doc">doc types</a>.</p><h3 id="addurls" tabindex="-1"><code>addUrls()</code> <a class="header-anchor" href="#addurls" aria-label="Permalink to &quot;`addUrls()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.addUrls(false)</span></span></code></pre></div><p><strong>Default value:</strong> <code>true</code></p><p>The <code>addUrls</code> builder controls whether to invoke the <code>addUrls</code> method for doc types included in the query results. If set to <code>false</code>, <code>addUrls</code> methods are not invoked.</p><p>This effectively controls whether the query results should include a <code>_url</code> property on individual documents, when available. <code>_url</code> is a dynamic property (not stored in the database) containing the document&#39;s unique URL. Not all documents have a unique URL.</p><p>Only piece type modules have an <code>addUrls</code> method in Apostrophe core. By default, page documents always include the <code>_url</code> property.</p><h3 id="and" tabindex="-1"><code>and()</code> <a class="header-anchor" href="#and" aria-label="Permalink to &quot;`and()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.and({ price: { $gte: 0 } })</span></span></code></pre></div><p>The <code>and</code> builder adds additional required criteria to the query. The value should be a MongoDB criteria object (as you would use in the <a href="https://docs.mongodb.com/v4.4/reference/method/db.collection.find/" target="_blank" rel="noreferrer"><code>find()</code> operation</a>). This is the main way to add criteria to a database query. It can be used multiple times to continue adding criteria.</p><p>Since this is the main way additional criteria get merged, this method performs a few transformations of the query to make it more readable when <code>APOS_LOG_ALL_QUERIES=1</code> is in the environment.</p><h3 id="archived" tabindex="-1"><code>archived()</code> <a class="header-anchor" href="#archived" aria-label="Permalink to &quot;`archived()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.archived(false)</span></span></code></pre></div><p>The <code>archived</code> builder tells the query whether or not to include archived documents in the results. It takes boolean value, <code>undefined</code>, or <code>null</code> (empty).</p><p>If the builder value is <code>false</code>, <code>undefined</code> or this method is never called, the query only returns docs that are <em>not</em> in the archive. This is the default behavior for document queries.</p><p>If the value is <code>true</code> the query returns <em>only</em> docs in the archive. Note that permissions would still prevent a typical site visitor from obtaining any results, but an editor might.</p><p>If the value is <code>null</code> (not <code>undefined</code>), the query returns docs regardless of archived status.</p><h3 id="areas" tabindex="-1"><code>areas()</code> <a class="header-anchor" href="#areas" aria-label="Permalink to &quot;`areas()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.areas(false)</span></span>\n<span class="line"><span style="color:#A6ACCD;"></span></span>\n<span class="line"><span style="color:#A6ACCD;">query.areas([ &#39;thumbnail&#39; ])</span></span></code></pre></div><p><strong>Default value:</strong> <code>true</code></p><p>The <code>areas</code> builder controls whether to call the <code>load</code> methods of widget type managers for widgets in areas. <code>load</code> methods are used to do things such as populating relationships in widgets and other asynchronous actions when areas are loaded.</p><p>The default value is <code>true</code>. Pass <code>false</code> to prevent the extra processing for a particular query. This is helpful to make queries faster when detailed area data is not needed.</p><p>The value can also be an array containing strings representing area field names or dot paths to nested area fields. For example, with <code>.areas([ &#39;thumbnail&#39; ])</code> you could load only the <code>&#39;thumbnail&#39;</code> area for all pages matching the query.</p><h3 id="attachments" tabindex="-1"><code>attachments()</code> <a class="header-anchor" href="#attachments" aria-label="Permalink to &quot;`attachments()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.attachments(true)</span></span></code></pre></div><p><strong>Default value:</strong> <code>false</code></p><p>Passing <code>true</code> to the <code>attachments</code> builder will annotate all attachment fields in the returned documents with URLs and other metadata (primarily for images). This uses the the <code>apos.attachment.all</code> method with the <code>annotate: true</code> option.</p><h3 id="autocomplete" tabindex="-1"><code>autocomplete()</code> <a class="header-anchor" href="#autocomplete" aria-label="Permalink to &quot;`autocomplete()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.autocomplete(&#39;tree&#39;)</span></span></code></pre></div><p>The <code>autocomplete</code> builder operates as a sort of fuzzy search for documents. It accepts a string and uses document search text properties to find matches. The string can contain multiple words, but only the final word may be a partial string to find non-exact results. So &quot;tree&quot; will find results with &quot;treehouse,&quot; but &quot;treehouses&quot; will not find &quot;treehouse&quot; (it is not meant as an advanced search tool, but can help find similar results).</p><p>This will only find partial matches in high-priority properties such as the <code>title</code>, or any string, select or checkbox field in the schema of the document.</p><h3 id="criteria" tabindex="-1"><code>criteria()</code> <a class="header-anchor" href="#criteria" aria-label="Permalink to &quot;`criteria()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.criteria({ category: { $in: [&#39;animals&#39;, &#39;vegetables&#39;] } })</span></span></code></pre></div><p><strong>Default value:</strong> <code>{}</code></p><p>The <code>criteria</code> builder sets the base MongoDB query criteria, <strong>discarding criteria previously added</strong> using this method or the <a href="#and"><code>and</code> builder</a>. The <code>and</code> builder is usually better for most cases since it is not destructive. The default value of this builder is an empty object. As with the <code>and</code> builder, the criteria format would match that used in the <a href="https://docs.mongodb.com/v4.4/reference/method/db.collection.find/" target="_blank" rel="noreferrer">MongoDB <code>find</code> operation</a>.</p><h3 id="defaultsort" tabindex="-1"><code>defaultSort()</code> <a class="header-anchor" href="#defaultsort" aria-label="Permalink to &quot;`defaultSort()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.defaultSort({ updatedAt: 1 })</span></span></code></pre></div><p>The <code>defaultSort</code> changes the default value for the <a href="#sort"><code>sort</code> query builder</a>. The argument is the same as for the <code>sort</code> query builder: an object like <code>{ updatedAt: 1 }</code>, including a field name and <code>1</code> for ascending or <code>-1</code> for descending. See the <a href="https://docs.mongodb.com/manual/reference/method/cursor.sort/" target="_blank" rel="noreferrer">MongoDB <code>sort</code> method documentation</a> for other options. <code>false</code> can be passed as an option as well to clear the default sort.</p><p>It is distinct from the <code>sort</code> builder to distinguish between cases where a default sort should be ignored (for instance, the <code>search</code> query builder is present) and cases where a sort is explicitly requested by the user.</p><p>In most custom uses the <code>sort</code> builder will likely be a more common option.</p><h3 id="ids" tabindex="-1"><code>_ids()</code> <a class="header-anchor" href="#ids" aria-label="Permalink to &quot;`_ids()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query._ids([</span></span>\n<span class="line"><span style="color:#A6ACCD;">  &#39;ckwxzxdtr000t4v3rivysoari:en:published&#39;,</span></span>\n<span class="line"><span style="color:#A6ACCD;">  &#39;ckwxzxdvc001b4v3rhxvxqgg1:en:published&#39;</span></span>\n<span class="line"><span style="color:#A6ACCD;">])</span></span></code></pre></div><p>The <code>_ids</code> builder causes the query to return only the document with matching <code>_id</code> properties and to return them in that order, assuming the documents with the specified IDs exist. All documents are fetched in the locale and mode of the request regardless of the locale suffix of the values. If no locale can be determined via query parameters, the locale is inferred from the first _id in the set.</p><p>The query builder can also be called with a string, which is treated as a single document ID.</p><h3 id="limit" tabindex="-1"><code>limit()</code> <a class="header-anchor" href="#limit" aria-label="Permalink to &quot;`limit()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.limit(10)</span></span></code></pre></div><p>The <code>limit</code> query builder accepts an integer and limits the number of document results. It returns the first matching documents up to the set limit after taking the <a href="#skip"><code>skip</code> builder</a> into account.</p><h3 id="locale" tabindex="-1"><code>locale()</code> <a class="header-anchor" href="#locale" aria-label="Permalink to &quot;`locale()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.locale(&#39;es:published&#39;)</span></span></code></pre></div><p><strong>Default value:</strong> <code>false</code></p><p>A valid locale identifier passed to the <code>locale</code> builder will tell the query to return results only belonging to that locale, including the mode (<code>draft</code> or <code>published</code>) after a <code>:</code> character. Documents that match the query and have <em>no locale</em> (because their doc type is not localized) are also included in the results. Specifically passing <code>false</code> (the default value) will use the locale and mode on the <code>req</code> request object.</p><h3 id="log" tabindex="-1"><code>log()</code> <a class="header-anchor" href="#log" aria-label="Permalink to &quot;`log()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.log(true)</span></span></code></pre></div><p><strong>Default value:</strong> <code>false</code></p><p>Setting the builder <code>.log(true)</code> on a query will log the the query criteria on the server (or the terminal when working on a local machine).</p><h3 id="next" tabindex="-1"><code>next()</code> <a class="header-anchor" href="#next" aria-label="Permalink to &quot;`next()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.next({</span></span>\n<span class="line"><span style="color:#A6ACCD;">  _id: &#39;cgwxzcdvc501b4v3ghxvxqgg1:en:published&#39;,</span></span>\n<span class="line"><span style="color:#A6ACCD;">  ...</span></span>\n<span class="line"><span style="color:#A6ACCD;">})</span></span></code></pre></div><p><strong>Default value:</strong> <code>false</code></p><p>Passing a document object to the <code>next</code> builder returns the document that follows it in the current sort order. The document object argument needs to at least include the properties used by the current sort as well as the <code>_id</code>. The <code>_id</code> is used as a tiebreaker to avoid loops.</p><h3 id="page" tabindex="-1"><code>page()</code> <a class="header-anchor" href="#page" aria-label="Permalink to &quot;`page()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.page(5)</span></span></code></pre></div><p><strong>Default value:</strong> <code>1</code></p><p>The <code>page</code> builder is used to request a particular &quot;page,&quot; or set, of results when the <a href="#perpage"><code>perPage</code> builder</a> is also used. Page numbers start with <code>1</code> (there is no page zero).</p><h3 id="pageurl" tabindex="-1"><code>pageUrl()</code> <a class="header-anchor" href="#pageurl" aria-label="Permalink to &quot;`pageUrl()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.pageUrl(false)</span></span></code></pre></div><p><strong>Default value:</strong> <code>true</code></p><p>The <code>pageUrl</code> query builder is set to <code>true</code> by default and controls whether to add the <code>._url</code> property to page documents in the query results. The builder can be set to <code>false</code> to disable the <code>._url</code> property on a particular query.</p><p>The <code>._url</code> property will include a site prefix if applicable and is always better to use than the document&#39;s <code>slug</code> for a URL.</p><h3 id="permission" tabindex="-1"><code>permission()</code> <a class="header-anchor" href="#permission" aria-label="Permalink to &quot;`permission()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.permission(&#39;edit&#39;)</span></span></code></pre></div><p>The <code>permission</code> builder is used to restrict returned documents based on the action name passed as an argument. Only documents on which the <code>req</code> object can take the named action are returned. For example, using <code>.permission(&#39;edit&#39;)</code> on the query will only return documents that the requesting user (via <code>req</code>) can edit.</p><p>Valid action arguments include:</p><ul><li><code>&#39;view&#39;</code>: The <code>req</code> has permission to view the documents. This is the default.</li><li><code>&#39;edit&#39;</code>: The <code>req</code> has permission to edit the documents.</li><li><code>&#39;publish&#39;</code>: The <code>req</code> has permission to publish the documents.</li><li><code>false</code>: Bypass any permission checks. This returns everything regardless of permission level. <em>Use this with caution.</em></li></ul><p>In all cases, all of the returned docs are marked with <code>_edit: true</code> properties if the user associated with the request is allowed to edit the document and <code>_published: true</code> if the user is allowed to publish it. This is useful if you are fetching docs for viewing but also want to know which ones can be edited.</p><h3 id="perpage" tabindex="-1"><code>perPage()</code> <a class="header-anchor" href="#perpage" aria-label="Permalink to &quot;`perPage()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.perPage(20)</span></span></code></pre></div><p><strong>Default value:</strong> <code>undefined</code></p><p>Using the <code>perPage</code> builder returns documents in sets of the number passed as an argument. This helps return documents in managable numbers and paginate the results, using the <a href="#page"><code>page</code> query builder</a> to get a specific set of results. This is usually easier than using <code>skip</code> and <code>limit</code> directly.</p><p>After the query completes, with <code>await query.toArray()</code> for instance, <code>query.get(&#39;totalPages&#39;)</code> will return the total number of pages.</p><h3 id="previous" tabindex="-1"><code>previous()</code> <a class="header-anchor" href="#previous" aria-label="Permalink to &quot;`previous()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.previous({</span></span>\n<span class="line"><span style="color:#A6ACCD;">  _id: &#39;cgwxzcdvc501b4v3ghxvxqgg1:en:published&#39;,</span></span>\n<span class="line"><span style="color:#A6ACCD;">  ...</span></span>\n<span class="line"><span style="color:#A6ACCD;">})</span></span></code></pre></div><p><strong>Default value:</strong> <code>false</code></p><p>Passing a document object to the <code>previous</code> builder returns the document that precedes it in the current sort order. The document object argument needs to at least include the properties used by the current sort as well as the <code>_id</code>. The <code>_id</code> is used as a tiebreaker to avoid loops.</p><h3 id="project" tabindex="-1"><code>project()</code> <a class="header-anchor" href="#project" aria-label="Permalink to &quot;`project()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.project({</span></span>\n<span class="line"><span style="color:#A6ACCD;">  title: 1,</span></span>\n<span class="line"><span style="color:#A6ACCD;">  category: 1,</span></span>\n<span class="line"><span style="color:#A6ACCD;">  updatedAt: 1</span></span>\n<span class="line"><span style="color:#A6ACCD;">})</span></span></code></pre></div><p>The <code>project</code> builders sets the <a href="https://docs.mongodb.com/manual/tutorial/project-fields-from-query-results/" target="_blank" rel="noreferrer">MongoDB projection</a>, specifying the document properties to included in the returned documents. The argument should be an object with properties of desired field names set to <code>1</code> to include those fields. Negative projections (<code>category: 0</code>) are currently not supported.</p><p>In addition to database properties, Apostrophe provides two dynamic properties that can be included in the <code>project</code> builder object:</p><ul><li><code>_url: 1</code> will include all properties required to generate an accurate <code>_url</code> property for each returned document.</li><li><code>_relationshipName: 1</code> will add the properties required to permit a field of type <code>relationship</code> to be loaded, even though the related documents are not stored redundantly in the database. In this example, the name of the field is <code>_relationshipName</code>. <code>_articles: 1</code> could do the same for a relationship to <code>article</code> pieces.</li></ul><h3 id="relationships" tabindex="-1"><code>relationships()</code> <a class="header-anchor" href="#relationships" aria-label="Permalink to &quot;`relationships()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.relationships(false)</span></span>\n<span class="line"><span style="color:#A6ACCD;"></span></span>\n<span class="line"><span style="color:#A6ACCD;">query.relationships([&#39;_author&#39;])</span></span></code></pre></div><p><strong>Default value:</strong> <code>true</code></p><p>By default, <a href="/guide/relationships.html">relationship</a> data are fetched for queried documents. The <code>relationships</code> builder can prevent or limit this.</p><p>Pass <code>false</code> to the builder to not retrieve any relationship data. You may also pass an array of relationship names, which will fetch only those relationships and those intermediate to them (using dot notation).</p><h3 id="sort" tabindex="-1"><code>sort()</code> <a class="header-anchor" href="#sort" aria-label="Permalink to &quot;`sort()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.sort({ updatedAt: -1, title: 1 })</span></span></code></pre></div><p>The <code>sort</code> builder sets the sorting order for returned documents. If not set, the default is used, from the <a href="#defaultsort"><code>defaultSort</code> builder</a> or module configuration. The sort argument is an object like <code>{ updatedAt: 1 }</code>, including a field name and <code>1</code> for ascending or <code>-1</code> for descending. See the <a href="https://docs.mongodb.com/manual/reference/method/cursor.sort/" target="_blank" rel="noreferrer">MongoDB <code>sort</code> method documentation</a> for other options.</p><p><code>false</code> can be passed as an option to use <em>no sort definition at all</em>. This can be helpful when using MongoDB operators like <code>$near</code>, which already sort.</p><p>If this method is never called or the argument is <code>undefined</code>, a case-insensitive sort on the title is normally the default. If <code>search()</code> has been called, then a sort by search result quality is the default.</p><p>If the query sorts on a field that is defined in a doc type&#39;s schema with the <code>sortify: true</code> option, then this query builder will automatically substitute a &quot;sortified&quot; version of the field: case-insensitive and ignoring extra whitespace and punctuation. This provides a more natural sort than MongoDB normally does.</p><h3 id="skip" tabindex="-1"><code>skip()</code> <a class="header-anchor" href="#skip" aria-label="Permalink to &quot;`skip()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.skip(10)</span></span></code></pre></div><p>The <code>skip</code> builder accepts a number as an argument, then skips that number of documents in a query&#39;s results. This affects <code>toArray</code> and <code>toObject</code>. It does not affect <code>toDistinct</code> or <code>toMongo</code>.</p><h3 id="search" tabindex="-1"><code>search()</code> <a class="header-anchor" href="#search" aria-label="Permalink to &quot;`search()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.search(&#39;tree&#39;)</span></span></code></pre></div><p>The <code>search</code> builder limits results to those matching the string passed as an argument. Search is implemented using MongoDB&#39;s <code>$text</code> operator and a full text index. The <code>search</code> builder does not support partial matches. For that, see the <a href="#autocomplete"><code>autocomplete</code> builder</a>.</p><p>If this query builder is set, the <code>sort</code> query builder will default to sorting by search quality. This is important because the worst of the full-text search matches will be of very poor quality.</p><h3 id="type" tabindex="-1"><code>type()</code> <a class="header-anchor" href="#type" aria-label="Permalink to &quot;`type()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.type(&#39;product&#39;)</span></span></code></pre></div><p>The <code>type</code> builder can be used to limit a query to only one particular doc type. Pass the doc type name (string) as an argument.</p><p>Generally you don&#39;t want to call this method directly. It will be better to use a doc type module&#39;s <code>find</code> method, which limits to that type automatically. You can also include the doc type name as the <code>type</code> criteria in a generic <code>find()</code> method&#39;s arguments.</p><h3 id="withpublished" tabindex="-1"><code>withPublished()</code> <a class="header-anchor" href="#withpublished" aria-label="Permalink to &quot;`withPublished()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.withPublished(true)</span></span></code></pre></div><p>If the <code>withPublished</code> builder is set to <code>true</code>, then each document in the results will include a <code>_publishedDoc</code> property. That property will be set to the published version of that document. This is really only useful when querying for draft documents.</p><h2 id="page-document-query-builders" tabindex="-1">Page document query builders <a class="header-anchor" href="#page-document-query-builders" aria-label="Permalink to &quot;Page document query builders&quot;">​</a></h2><p>The following query builders are only available on query created with the page module&#39;s <code>apos.page.find</code> method and <a href="/reference/api/pages.html">page REST API routes</a>.</p><h3 id="ancestors" tabindex="-1"><code>ancestors()</code> <a class="header-anchor" href="#ancestors" aria-label="Permalink to &quot;`ancestors()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.ancestors(true)</span></span>\n<span class="line"><span style="color:#A6ACCD;"></span></span>\n<span class="line"><span style="color:#A6ACCD;">query.ancestors({ children: true })</span></span></code></pre></div><p><strong>Default value:</strong> <code>false</code></p><p>Setting the <code>ancestors</code> builder to <code>true</code> retrieves the array of &quot;ancestors&quot; for each returned page and assigns them to the <code>_ancestors</code> property. Page ancestors are those that precede a given page in the page tree hierarchy. The home page is <code>_ancestors[0]</code>. A returned page is not included in its own <code>_ancestors</code> array.</p><p>If the builder&#39;s argument is an object, the builder does all of the above, and also calls the query builders present in the object <em>on the query that fetches the ancestors</em>. For example, you can pass <code>{ children: true }</code> to fetch the children of each ancestor as the <code>_children</code> property of each ancestor, or pass <code>{ children: { depth: 2 } }</code> to get two layers of child pages.</p><p><code>ancestors</code> also has its own <code>depth</code> option, but it might not do what you think. If the <code>depth</code> option is present as a top-level property of the <code>ancestors</code> builder argument, then only that number of ancestors are retrieved, counting backwards from the immediate parent of each page. So <code>{ depth: 2 }</code> retrieves only the two closest ancestors.</p><h3 id="children" tabindex="-1"><code>children()</code> <a class="header-anchor" href="#children" aria-label="Permalink to &quot;`children()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.children(true)</span></span>\n<span class="line"><span style="color:#A6ACCD;"></span></span>\n<span class="line"><span style="color:#A6ACCD;">query.children({ depth: 1 })</span></span></code></pre></div><p><strong>Default value:</strong> <code>false</code></p><p>The <code>children</code> builder is used to include the &quot;children&quot; of returned pages in an array on a <code>_children</code> property. If <code>children(true)</code> is called, it will return all children of a given page. If the argument is an object, it may have a <code>depth</code> property to fetch a specific number of child layers. Any other properties are passed on <em>as builders for the query that fetches the children</em>.</p><h3 id="ispage" tabindex="-1"><code>isPage()</code> <a class="header-anchor" href="#ispage" aria-label="Permalink to &quot;`isPage()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.isPage(true)</span></span></code></pre></div><p><strong>Default value:</strong> <code>true</code></p><p>Passing <code>true</code> to the <code>isPage</code> builder ensures that results will only include documents that are pages.</p><h3 id="orphan" tabindex="-1"><code>orphan()</code> <a class="header-anchor" href="#orphan" aria-label="Permalink to &quot;`orphan()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.orphan(true)</span></span></code></pre></div><p>The <code>orphan</code> builder is used to return documents based on their &quot;orphan&quot; status. Orphans are pages that are not returned by the default behavior of the <code>children</code> query builder and thus are left out of standard navigation.</p><p>If flag is <code>true</code>, return only orphan docs. If flag is <code>false</code>, return only docs that are not orphans. If <code>.orphan(null)</code> or <code>undefined</code> is called, or this method is never called, return docs regardless of orphan status.</p><h2 id="image-document-query-builders" tabindex="-1">Image document query builders <a class="header-anchor" href="#image-document-query-builders" aria-label="Permalink to &quot;Image document query builders&quot;">​</a></h2><p>The following query builders are only available on query created with the image module&#39;s <code>apos.image.find</code> method and <a href="/reference/api/pieces.html">piece REST API routes</a> when dealing with the <code>@apostrophecms/image</code> piece type.</p><h3 id="minsize" tabindex="-1"><code>minSize()</code> <a class="header-anchor" href="#minsize" aria-label="Permalink to &quot;`minSize()`&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">query.minSize([ 600, 800 ])</span></span></code></pre></div><p>The <code>minSize</code> query builder can be used to set a minimum size for returned image documents. The argument should be an array with two values: the image width and the image height, in that order. The sizes are measured in pixels.</p>',142),n=[r];function l(d,i,c,p,h,u){return a(),t("div",null,n)}const y=e(s,[["render",l]]);export{g as __pageData,y as default};
